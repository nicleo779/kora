<mapper namespace="com.nicleo.kora.quarkus.deployment.test.mapper.TestUserMapper">
    <select id="selectById">
        select id, name
        from test_user
        where id = #{id}
    </select>
</mapper>
